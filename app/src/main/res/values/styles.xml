<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- Стиль для подписей внизу крестиков-ноликов -->
    <style name="TicTacBottomLabel">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Стили для chip-ов с типами поддерживаемых игр -->
    <style name="GameTypeChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="android:checkable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="chipBackgroundColor">@color/game_type_background</item>
        <item name="android:textColor">@color/game_type_text</item>
    </style>

</resources>
